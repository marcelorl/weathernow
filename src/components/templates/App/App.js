import React from 'react'
import styled from 'styled-components'

// import Header from '../../atoms/Header'

const AppContainer = styled.div`
  a {
    background: #f1f1f1;
  }
`

const App = ({ products }) =>
  <AppContainer>ssdf</AppContainer>

export default App
